buildscript {
     repositories {
         flatDir {
             dirs 'libs'
         }
     }
 }

 repositories {
     flatDir {
         dirs 'libs'
     }
 }
 dependencies {
     compile fileTree(dir: 'libs', include: ['*.jar'])
     compile(name:'fraudforce-lib-release-4.3.0', ext:'aar')
 }